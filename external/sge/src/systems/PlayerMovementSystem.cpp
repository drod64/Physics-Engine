#include <SGE/systems/PlayerMovementSystem.h>

void sge::PlayerMovementSystem::update(Registry &registry, CommandBuffer &, sm::real dt)
{
    // Only look for Entities with specific tags or components.
    
    
}